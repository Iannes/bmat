import { makeStyles } from "@material-ui/core/styles"

export const useStyles = makeStyles(() => ({
  root: {
    flexGrow: 1
  },
  input: {
    width: "100%"
  },
  container: {
    flexGrow: 1,
    position: "relative"
  }
}))
